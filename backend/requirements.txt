# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-dotenv==1.0.1

# HTTP client (for Anthropic API calls)
httpx==0.27.2

# Data processing (Agents 1 & 2)
pandas==2.2.3
numpy==2.1.1

# Agent orchestration
langgraph==0.2.28
langchain-core==0.3.10

# WhatsApp delivery (optional — for real WhatsApp on demo day)
# Run: pip install twilio
# twilio==9.3.0

# Already installed in Python stdlib — no extra deps needed for:
# sqlite3 (session store), urllib (HTTP fallback), re (intent classifier)
